<linker>
  <!-- Runtime-authored organic meshes add collider types internally. These
       references are not visible to the managed linker, so the optimized WebGL
       edition retains the physics module explicitly. -->
  <assembly fullname="UnityEngine.PhysicsModule" preserve="all" />
</linker>
